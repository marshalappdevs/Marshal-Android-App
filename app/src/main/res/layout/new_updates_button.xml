<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:gravity="center"
    android:background="@drawable/new_updates_background"
    android:paddingLeft="22.0dip"
    android:paddingRight="22.0dip"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:foreground="?android:selectableItemBackground">

    <ImageView
        android:id="@+id/new_updates_arrow"
        android:layout_width="wrap_content"
        android:layout_height="16.0dip"
        app:srcCompat="@drawable/ic_action_refresh"
        android:contentDescription="@null" />
    <TextView
        android:textSize="12.0dip"
        android:textColor="@android:color/white"
        android:id="@+id/new_updates_button_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/new_updates"
        android:layout_marginStart="3.0dip" />
</LinearLayout>