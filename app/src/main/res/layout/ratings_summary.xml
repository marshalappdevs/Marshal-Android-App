<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/ratings_summary_box"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:focusable="true"
    android:baselineAlignedChildIndex="1">

    <RatingBar
        style="?android:attr/ratingBarStyleSmall"
        android:id="@+id/summary_rating_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="false"
        android:numStars="5"
        android:stepSize="0.5"
        android:rating="0"
        android:theme="@style/Course.RatingBar" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/course_content_textView_ratingsAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:includeFontPadding="false"
            android:textColor="?android:textColorSecondary"
            android:text="0" />

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/course_content_textView_ratingsAmount"
            android:layout_marginStart="3.0dp"
            app:srcCompat="@drawable/ic_user_rating"
            android:baselineAlignBottom="true"
            android:layout_alignBaseline="@id/course_content_textView_ratingsAmount"
            android:contentDescription="@null" />

    </RelativeLayout>
</LinearLayout>
